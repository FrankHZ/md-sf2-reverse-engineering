# Remake Capability Status

## Reading This Matrix

This matrix groups coherent runtime capabilities. It is not a pull-request chronology and does not
create evidence for the original game.

Status terms:

- **Implemented:** available through the named bounded profile and covered by owning tests or smoke.
- **Admitted prerequisite:** trusted typed data can enter the named inner boundary but is not consumed
  by a runnable product path.
- **Unsupported:** the current profile deliberately has no implementation for the capability.
- **Unknown:** accepted evidence or product behavior is still incomplete at another owner.

## Current Capabilities

| Capability | Owner and surface | Status | Retained boundary |
| --- | --- | --- | --- |
| Public-synthetic Map 3 admission and exploration | Content package, Domain movement, Application `GameSession`, Godot viewport and action/status deck | **Implemented** | synthetic layout and walkability plus project-authored oriented/entity/blocked glyphs only; no original collision, route, coordinates, or presentation claim |
| Public-synthetic context and interaction stack | setup/area/zone selection plus event/effect, local transition, entity/dialogue, search/discovery, and item-acquisition lifecycles | **Implemented** | project-authored identities, text, flags, items, cues, and effects; no original meaning or natural reach |
| Public-synthetic outbound map transition | Application transition lifecycle and exact runtime catalog lookup | **Implemented** | destination is a tiny synthetic shell, not original Map 21 or an original warp |
| Public-synthetic tactical battle depth | pure Domain 3-by-2 loop, deterministic single enemy response, typed victory/defeat and same-definition retry, Content definition, Application `GameSession` lifecycle, once-only victory world state, and thin Godot projection | **Implemented** | project-authored actor/enemy/grid/ranges/HP/damage/tie-break/flag/effect/setup/cues only; not Battle 01, original AI/combat/damage/RNG/rewards/after-program, natural admission, or fidelity |
| Public-synthetic Godot and export smoke | thin Godot host and maintained local official-engine gate | **Implemented** | stable synthetic receipt only; no online Godot job and no release authorization |
| Private canonical Map 3/19/20 admission and traversal | private Content import, immutable Application runtime catalog, private session, Domain `OriginalMapTraversal` and block-copy lifecycle, exact same-map warps, bounded Map 3 route records, north Map 19 transition, and royal Map 20 transition | **Implemented** | controlled Map 3 start, bounded castle-gate route, exact north transition, and the 38-input Map 19 entry-to-royal route through two default RTS zones and the stair exit; Map 20 arrival is `(23,37)`, area 2, opaque facing 3, explicitly before init, with bounded traversal and an explicit current-runtime base view (traversal-only launches retain diagnostics); Map 19/20 setup/init/generic event/entity execution remains Unsupported, natural caller state and Battle 01 continuity remain Unknown, and no original rendering, timing, persistence, general script engine, or presentation-fidelity claim is added |
| Private selected-setup entity population | fixed canonical Content admission plus immutable Application import definition and private snapshot | **Implemented as typed session context with bounded live exceptions** | ordered source identity, masked position, opaque facing/map-sprite/tail, plus bespoke session-owned Zone 601, Sarah, Entity 142, and messenger-result state; the accepted Astral handoff repositions live route actors, messenger acceptance releases Sarah/Entity 142 occupancy and records two guard endpoints, while generic visibility, walking, AI, action interpretation, entity-143 rendering, natural reach, and H3 population remain unclaimed |
| Private traversal diagnostics | typed viewport, controlled one-shot working-layout copy, current area, current block catalog | **Implemented** | project-authored diagnostic display; no natural trigger, OpenDoor, camera, layer, or visible-original claim |
| Private visual resource references | canonical import selection for palette and ordered tileset slots | **Implemented as typed metadata** | identities only; no payload, placement, cache, animation, or renderer semantics |
| Private base visual payload | sibling private Content port and immutable Application definition | **Admitted offline prerequisite** | fixed-ROM decoded buffers and palette forms remain local/in-memory for tooling and admission checks; playable startup does not consume them |
| Private visual runtime binding | Application cross-port compatibility and private session construction | **Implemented but unconsumed by playable startup** | retained as offline protocol/admission support; no payload enters PCK, status, or smoke |
| Private project-authored Map 3 base view | Godot-only main-layer composition over the authoritative private session and reviewed local asset pack | **Implemented** | 12-by-7 clamped crop, project-authored background/color display and player marker; playable startup requires no ROM/extraction metadata and makes no original camera, overlay, animation, or final-pixel claim |
| Private-local project-authored battle bridge | Application-owned manual bridge at the controlled start and the existing thin Godot tactical projection | **Implemented** | reuses the synthetic battle definition/reducer, preserves the exact private traversal snapshot through defeat/retry and victory, and never applies the public world effect; no natural admission, Battle 01, original after-program, or fidelity claim |
| Local presentation asset-pack admission and bounded semantic HUD/battle overlays | Application path-free typed catalog/receipt, Content-owned manifest/path/payload reader, explicit PrivateLocal profile values, and thin Godot-only projection nodes | **Implemented as bounded local consumers** | exact semantic frame and tactical-cursor assets with accepted 2x/4x selection, chrome-only fallback, one private battle-entry ENTER/STAY panel, and an additive typed-`HasCursor` ring retaining the gold-cell and `▣` fallback; no original Yes/No/cursor behavior, product font, Theme, generic service, PCK asset, smoke marker, or fidelity claim |
| Private Map 3 local-asset runtime | exact reviewed local asset transaction, explicit PrivateLocal base-view plus atlas selection, Content-owned payload recheck, and Godot-only physical-raster atlas projection | **Implemented as bounded playable local consumer** | the admitted 2x/4x atlas is presentation-authoritative and validated as exact-nearest before any logical collapse; ROM/tileset/palette metadata remain offline builder inputs, source/master never enter runtime or PCK, and no camera/layer/animation/hardware/display/final-pixel or fidelity claim is added |
| Private Map 3 static overlay diagnostic | explicit PrivateLocal base-view option, admitted typed area catalog and current working layout, and the existing thin Godot projection | **Implemented as bounded local consumer** | derives the sole non-zero area offset, uses the admitted map palette as a declared diagnostic policy, preserves index-zero holes, draws no player/entity marker, and reprojects only the atomically committed current layout; original background palette, sprite priority, camera/parallax/autoscroll/layer switching, generalized roof/setup/init execution, animation, timing, and final pixels remain Unknown |
| Private Map 3 current-area second layer | explicit PrivateLocal base-view option, authoritative private snapshot and existing Godot atlas/block projector | **Implemented as modern bounded composition** | a zero foreground/background delta remains main-only; a non-zero delta samples the current working layout, keeps block-index-zero holes transparent, and is reprojected before entity/player overlays after committed session updates; original planes, priority, palette switching, parallax/autoscroll, roof and animation timing, and final pixels remain Unknown |
| PrivateLocal product startup display scale | root Godot window policy plus the accepted presentation bucket selector | **Implemented** | adaptive fitting windowed client up to 1920 by 1080, runtime 960-by-540 minimum, explicit target preservation, centered `keep` aspect and HiDPI without double counting; one startup-resident 2x/4x bucket, with live remount and settings UI deferred |
| Local presentation checkout/export preflight | repository-owned path-free descriptor, clean local-Git identity check, and atomic runtime-only exporter | **Implemented tooling prerequisite** | requires explicit commit/tree/manifest pins and an already reviewed pack; it does not generate, promote, or commit assets; no Godot consumer, remote backup, or product fallback |
| HUD SVG candidate derivation | pinned resvg archive/version, closed static-SVG policy, deterministic 2x/4x RGBA8 build, existing pack schema, and ignored-cache atomic publisher | **Implemented tooling prerequisite** | permits one exact untracked candidate over an otherwise clean checkout and changes ignored cache only; the separately reviewed first frame transaction and consumer do not turn this builder into a promotion, commit, update, or general HUD pipeline |
| Private Map 3 base-atlas candidate derivation | fixed ROM/tileset/palette roots, exact five-slot selection, accepted Stack/4bpp/palette helpers, one 128-by-320 master, and nearest 2x/4x ignored-cache outputs | **Implemented tooling prerequisite** | emits only an ignored review candidate; the separately reviewed atlas transaction and consumer do not make the builder a promotion/update path or authorize xBRZ/color treatment, PCK, hardware/display/final-pixel parity, or redistribution |
| Private Map 3 controlled-player locomotion consumer | fixed-root reference and three-sheet candidate builders, reviewed local source/master/runtime/manifest transaction, Application-owned facing/counter/half/motion phase, exact Content-owned lookup, and thin Godot logical-cell projection | **Implemented as bounded local consumer** | admission uses the observed DOWN half/counter state; blocked attempts and successful 13-tick cycles retain the bounded accepted ordering and RIGHT mirror without `SimulationStep` parity; universal idle/standing meaning, arbitrary-route cadence, DMA/cache completion, original-camera, hardware/display/final-pixel parity, H4/8C, and redistribution remain unclaimed |
| Private Map 3 entity-142 two-half reference candidate | fixed-ROM and accepted-fixture candidate builder plus reviewed local source/master/runtime/manifest transaction | **Implemented tooling prerequisite** | exact entity/slot/map-sprite/UP source and both 24-by-24 halves remain source-ordered in one 48-by-24 sheet; the candidate itself owns no half selection, idle/counter/cadence/visibility/interaction meaning, animation, or fidelity claim |
| Private Map 3 entity-142 half-zero diagnostic | exact admitted entity population and record 17, reviewed two-half local asset, and thin Godot base-view projection | **Implemented as bounded local consumer** | `project-authored-half0-diagnostic-idle-v1` selects source-order half zero only for logical entity 142 / physical slot 17; no original-visible/idle/counter/cadence, lifecycle, walking, occupancy, collision, event, all-entity, or fidelity claim |
| Private Map 3 entity-142 two-half diagnostic animation | the existing exact entity-142 binding and reviewed two-half sheet plus viewport-owned immutable presentation state | **Implemented as bounded modern presentation policy** | `project-authored-two-half-diagnostic-cadence-v1` starts at half zero and holds each source-order half for 30 fixed Godot physics callbacks; it ignores delta, wall time, `SimulationStep`, and gameplay state and makes no original half/counter/cadence/visibility, generalized entity-animation, H4/8C, or fidelity claim |
| Private Map 3 live route-actor glyphs | the accepted session-owned Sarah and Zone 601 positions/facings plus the thin Godot base view | **Implemented as bounded modern presentation policy** | distinct project-authored diamond/square glyphs follow only these two live route exceptions; Sarah's glyph retires when Application marks her follower-ready and releases route occupancy; generic population visibility, original sprites, animation, AI, and fidelity remain unclaimed |
| Private Map 3 project-authored camera follow | Godot-only immutable focus/crop projection over the authoritative private snapshot and Application locomotion state | **Implemented as modern 9A presentation policy** | follows accepted 32-unit transitions as two logical pixels, centers at column 6/row 3 when unclamped, and samples the current working layout; original camera target/dead-zone/clamp, plane A/B, parallax/autoscroll, layer, VInt, hardware/display/final-pixel, H4/8C behavior remains Unknown |
| Full original visual presentation | no current product owner | **Unsupported** | original camera/layers/priority, color display, animation, UI, text, audio, timing, and final pixels remain outside the bounded project-authored base composition and atlas diagnostic |
| Controlled palace first-visit result and royal return | Content source binding, Application command/receipt and locomotion, thin Godot interaction/status | **Implemented** | Map 20 arrival stays pre-init and unselected; explicit F at `(23,37)` applies the clear-605/507 preset with the scene skipped, player `(23,39)`/DOWN, completion 605, entity-131 endpoint `(20,39)`, and entity-130 hidden receipt; two north inputs then return through warp record 5 to Map 19 `(23,3)`, area 1, raw facing 2/LEFT, retaining completion, route state and bridge; immutable source population stays unchanged; no natural flag resolution, general init/script/NPC execution, dialogue presentation, later F507 program, or other return warp |
| Controlled Map 19 Astral acceptance and passage | accepted H2 behavior contract, Content actor/event binding, Application completion/occupancy, thin Godot marker/action | **Implemented** | after the controlled royal return, 11 inputs reach `(16,6)` facing LEFT; blocked north faces UP/raw 1, F explicitly accepts with the scene skipped, actor 140 leaves occupancy and the next north reaches `(16,5)`; caller 607 and program 608 completion persist across movement/royal travel and reset on restart; canonical input does not contain the handler/program bodies, and no natural init, original prompt/dialogue/choreography, tower warp, Map 21 or Battle 01 is claimed |
| Controlled west-tower entry | exact Content warp binding, Application transition/receipt/snapshot and locomotion, thin Godot arrival status | **Implemented** | from released Map 19 `(16,5)`, 14 inputs reach the stair trigger `(6,2)` through `(5,3)` east; source warp record 1 relocates to Map 20 `(6,37)`, area 2, RIGHT/raw 0, with palace 605, caller 607/program 608, route and bridge state retained; the royal record 2 remains distinct; current base display uses admitted Map 20 data, with no new init/F507, west-tower return, Map 21 or natural-continuity claim |
| Private castle, middle-tower and Map 40 base views | accepted fixed asset pack, runtime-owned visual selection, current working layout/blocks/area and existing Godot camera/locomotion | **Implemented as bounded local consumer** | Map 3 swaps to the shared Map 19/20 atlas on castle entry, then to the independent Map 21 family with palette 0/slots [6,23,44,53,8], then Map 40 palette 3/slots [94,95,96,97,58]; exact assets are required, with no cross-family fallback; Map 3 overlays/entity142/Sarah/Zone601 remain scoped; Astral follows occupancy and Map 21 guard follows live occupancy as an undirected diamond; no new NPC art, original rendering, natural lifecycle or H4 claim |
| Controlled middle-tower Map 21 arrival | exact Content runtime/record-4 binding, Application atomic transition and thin Godot view | **Implemented** | three Left inputs from Map 20 (6,37) resolve (3,36) and arrive at Map 21 (3,16), area 1, RIGHT/raw 0; exact layout/blocks/area/default setup/entity population; controlled 605/607/608 and route/bridge state persist, fresh session resets; the base-atlas launch shows its separately accepted Map 21 family, while traversal-only mode retains its grid; arrival does not execute init or automatically apply guard F; NPC art, natural Battle 01 and H4 remain unclaimed |
| Controlled Map 21 middle-tower guard | accepted H2 behavior, Content actor/setup/event binding, Application receipt/occupancy and real F adapter | **Implemented** | at `(4,16)` facing RIGHT, F selects the post-Astral/local256-clear preset, skips text 579 and intermediate presentation, moves guard128 from `(5,16)` to `(6,16)` and records handler256 separately from StoryFlag1/F401; player position/facing remain unchanged by explicit remake policy, then Right/Up reaches `(5,15)`/UP; source population and prior state/bridge remain unchanged; diagnostic diamond has no facing claim; entity135 effect, general flags/init/dialogue, Map40 lifecycle and natural Battle01/H4 remain Unknown |
| Controlled Map 21 north exit to Map 40 | accepted H2 exit/data identities, exact five-runtime Content/source-port admission, Application transition and thin Godot view | **Implemented with bounded local base presentation** | from completed guard (5,15)/UP, 18 ordinary inputs reach (9,2) and trigger (9,1), record 2→Map40 (4,30)/UP/area1; explicit castle/palace/Astral/guard prerequisites and persistent route/bridge receipts; next ordinary move clears transient transition receipt; empty entity resource and independent palette3/slots [94,95,96,97,58] are admitted; explicit base launch requires the exact Map 40 atlas, crop (0,27), player UP/raw 1 and no retained guard; traversal-only mode remains diagnostic; status reflects the active view and unexecuted init; completed Map40→57 relocation and battle startup remain unsupported; natural continuity remains Unknown |
| Controlled Battle 01 pending admission | fixed Map57 destination and CheckBattle facts, canonical/source-port validation, Application pending result and thin Godot status | **Implemented before relocation** | exact event-marked Map40 wildcard row1 admits either terminal (14,12)/(15,12); selected route commits 27 inputs to (14,13)/UP and the final Up preserves the exact Map40 snapshot, step, locomotion, import/route receipts and bridge; destination Map57 (8,18)/UP, candidate1 and area (0,0,16,20), explicit F501/F88/F451-clear preset and guard StoryFlag1/F401; Map57 null setup/animation stays outside five exploration runtimes; pending blocks exploration commands and restart clears it; explicit N with selected startup inputs continues through the separate controlled consumer below; Pending itself does not relocate or claim natural continuity |
| Controlled Battle 01 startup input preparation | selected private placement/scene reader, existing Stack decoder, fixed Application definition, explicit party/RNG preset and GameSession preparation | **Implemented; consumed by controlled initialization** | exact battle1/map57 deployment (3 allies/6 enemies/3 regions/0 points), source-labeled AI/item/filler/region bytes, full 48-by-48 raw terrain and separate 16-by-20 area; GIZMO source baseline before difficulty; named effective party/status/equipment/spells, difficulty0/seed0x1234 comparison; exact current Pending and source identities retained through repeated preparation and rejection; immutable inputs, no pending consumption, relocation, step/flag mutation, active roster, terrain occupancy or turn order; explicit Godot N consumes its result |
| Controlled Battle 01 initialization | Domain deterministic initializer and Application exact Prepared/Pending commit | **Implemented before first round; consumed by Godot N** | battle-owned Map57/1, nine initialized combatants, copied raw48x48 terrain and independent occupancy; explicit effective ally policy, source GIZMO ATT7 to difficulty0 ATT8 once, clear seconds/regions/AI memory, F451 set with F401 retained and F501/F88 clear; old Map40 remains frozen provenance and exploration commands close; no region activation, round spawning, generated turn order, selected actor, battle action or natural-continuity claim |
| Controlled Battle 01 first-round entry | Domain activation/round helpers and Application exact current-snapshot commit | **Implemented order; consumed by first control** | current phase FirstRoundGenerated; separate tested mask7 and all-clear region flags for the baseline, preserved source AI bits and empty region-cutscene/spawn results; all9 living placed units enter signed stable64-slot ordering, current offset0; RNG RAM image0x00001234 uses high word0 and becomes0xA4991234 after27 calls, low word retained; candidate1 is computed data for the separate control API; source/initialization provenance retained, duplicate/stale/foreign/failing requests cannot advance state |
| Controlled Battle 01 first-player API | Domain control/weighted movement and Application select/confirm/cancel facades | **Implemented API and diagnostic Godot consumer** | exact real first candidate and status/activation branch; only its missing ally word0 supplied by named caller preset, auto-battle/opponent-control false; class4 HEALER12 costs with current MOV*2, opponent blocking and allied traversal, occupied-stop rejection; independent cursor/preview and provisional live position/occupancy, cancel restores origin; named remake preview policy preserves source-mask disagreement for research correction; N consumes actual Pending through the accepted chain; I/J/K/L, Space and Backspace project live nine-unit terrain/range/path/cost state; bounded seeded native recipe checks input and view; STAY completion is a separate consumer below; no other action commit, AI execution, original cancel animation or original scene fidelity |
| Controlled Battle01 Map57 base art | fixed admitted destination projection, existing atlas catalog and shared block sampler | **Implemented; diagnostic units** | explicit base-atlas plus selected Battle01 inputs requires the accepted palette8/[94,98,99,255,255] family; full 16-by-20 area at 24px/block with physical nearest 2x/4x raster, live nine-unit/range/path/cursor overlays and provisional confirm/cancel; unchanged-layout admission and used-slot3/4 rejection retain the unused seed-block limit; missing/wrong requested art fails visibly; no original roster art, layer/VRAM/animation/timing fidelity, natural continuity, attack resolution/victory or H4 closure |
| Controlled player STAY and next-player control | Domain ordered no-effect completion and shared candidate/movement entry, exact GameSession commits and Godot action choice | **Implemented through second player; consumed by first enemy standby** | separate Space commits with zero statuses, no equipped passive recovery, living/placed roster, consistent occupancy and both faction gates; actual player2 uses class1/CENTAUR2 and MOV*2 with independent move/cancel/STAY; offsets0 to2 to4 retain all64 slots/main RNG and both receipts; actual128 / OpponentAi is the handoff to the bounded enemy consumer; no general stat refresh, new-round loop or H4 claim |
| Controlled first inactive enemy standby | Domain thinking RNG/tables/source move string and shared no-effect completion | **Implemented strict first entry; consumed by first-round relay** | independent preset seed-copy1234 retained through both player turns; enemy128 inactive regular GIZMO uses Hovering6/MOV5 raw grid plus separate occupancy; RNG8/2/1 gives7/0/0 with61/85/1 steps, west move(7,3) to(6,3), memory0 to14h, tested-mask7 to0 and seed-copy3934; mainA4991234, effective stats, deployment, last targets and both prior receipts retained; EnemyTurnCompleted at offset6 exposes actual131 to the bounded continuation; first-entry failure retains exact second STAY |
| Controlled Battle01 first-round completion | Existing standby/next-player owners, exact per-actor GameSession commits and finite Godot relay | **Implemented first-round component; consumed by repeatable continuation** | remaining actual enemies131/133/129/130/132 use chained seed-copy, own memory and evolving occupancy; 1336 further generator steps finish seed-copy0134; late failure retains the last completed actor and receipt prefix; actual Bowie0 alone receives missing word0 and class0/REGULAR1 budget12, independent move/cancel/STAY; all nine linked receipts, stats, main RNG and original64 slots survive at offset18/FF; no attacks/spells, death/outcome, original animation, natural seed lifetime or H4 claim |
| Controlled repeated inactive rounds | Existing round/control/standby owners and bounded actual-candidate Godot dispatch | **Implemented through actual round3 control in the origin-STAY comparison** | one current64-slot buffer/cursor; immutable round numbers and linked prior receipts; unsupported activation rejects before flags/order/main RNG commit; each inactive enemy uses original anchor, live grid/occupancy and retained own memory/copy; round2 mainAA861234,967 thinking bytes,copy0034,memory04/04/04/24/34/04h; round3 main9BD71234; players move/cancel/STAY manually; late control failure keeps generated round, late enemy failure keeps last actor; no key/frame retry or natural continuity claim |
| Controlled activated-enemy pursuit | Domain `Battle01EnemyPursuit`, exact GameSession facade and actual-candidate Godot relay | **Implemented** | known GIZMO commandsets6/7 with empty action cohorts; target costs below128, source path/radius fallback and no-effect STAY; roster-only activation excludes deals-memory alias; the standalone pursuit API retains its typed physical-cohort boundary before priority/RNG/movement; Godot routes that boundary to the bounded physical consumer below |
| Controlled ordinary enemy physical attack | Domain `Battle01EnemyPhysicalAttack`, distinct completion policy, exact GameSession facade and thin Godot relay | **Implemented through actual round6 attack132 and Bowie0 control at9 HP** | real target priority/action/source path; explicit class0/Wooden Sword prowess3 profile; source hit/miss/critical and both follow-up rolls; atomic nonlethal effect replay, last-target/HP/main/thinking history, receipt51/raw12; HP persists through movement/cancel/STAY and later admitted AI/generation; true double/counter, lethal/status/curse/reward, other combat profiles, death/victory/return, natural RNG lifetime and original animation/timing remain unsupported or Unknown |
| Controlled manual player physical attack, first enemy defeat and awards | Domain `Battle01PlayerPhysicalAttack`, exact GameSession facade, existing selection and receipt chain | **Implemented through round7 enemy132 defeat and actual player1 movement/cancel** | named EXP0/gold0/kills0 supplement preserves both older presets; receipt52 HP5→2/EXP0→15, then actual131/132/133 and Bowie HP6; manual lethal hit3 retains overkill−3, EXP15→39, gold0→60/kills0→1; receipt59 records first worklist132/empty second list, both3/5 counts, HP0/unplaced source row, cleared occupancy and same64 slots; mainF7751234/copy0234; player1 HP11/budget10 and persistent visible defeat/awards; history validates accounting, placement and generation provenance; further defeats, true follow-ups, level transition, outcome/return, other profiles and original presentation/VInt RNG remain unsupported or Unknown |
| Continuous Map 3-through-Battle 01 milestone | Research, design contracts, future Application/Domain/Godot work | **Unknown / NOT READY** | bounded relocations reach Map 40 before init through explicit controlled palace/Astral results; natural caller state, remaining palace lifecycle, natural Battle 01 admission/startup, playthrough/victory, endpoint, complete private presentation, and complete acceptance remain open; the bridge-seeded Map 21 H3 seam does not close this route |
| Save/load and persistence | future milestone and save-system owner | **Unsupported / deferred** | no player-facing save, checkpoint, suspend, migration, or persistence adapter |
| Complete H4 and 8C reference parity | future layered observation owners | **Unknown** | exact reached frame, palette, audio, hardware chronology, capture conditions, and accepted tolerances remain open |

The playable presentation baseline fits the exploration action/status deck and the shared tactical
battle panel inside the fixed 960-by-540 host canvas. Controls are named beside the typed state they
affect; map and battle actors use color-independent glyphs and legends; battle HP, phase-specific next
actions, cues, victory, defeat, retry, and return are projected from immutable session snapshots or typed
command results. These are project-authored readability choices, not original UI, graphics, camera,
timing, or fidelity evidence.

## Stable Compatibility Surfaces

Existing public-synthetic and private-local smoke observations remain compatibility surfaces until a
separately reviewed migration changes them. The legacy `SF2_MAP3_SMOKE` receipt remains byte-stable
and precedes the additive public-synthetic battle receipt. Internal refactors must preserve their
marker bytes, order, profile disclosures, path-free output, and process-cleanup result.
The optional private base-atlas receipt is additive and final after the four established private
markers; it is absent unless the atlas is explicitly selected and exposes no local path or pixel data.

Future diagnostic facts should extend one bounded inspector or observation model. They do not receive a
new capability, receipt, or matrix row unless they independently cross a trust, authority, versioned-port,
or stable-observation boundary under
[ADR 0017](../../docs/decisions/0017-heavy-boundaries-light-internals.md).

## Current Engineering Frontier

The bounded Godot host extraction is complete: input, public/private presentation, and public/private
smoke drivers have focused owners. Profile selection and typed public/private startup remain
intentionally inline in the composition root because they join distinct ports and results. File size
alone is not a reason to reopen those boundaries.

The next engineering frontier is product capability behind `GameSession`, with internal Application or
Content decomposition only when an owning behavior change makes it necessary. The accepted bounded
castle-gate route crosses the exact north relocation into Map 19 and the royal stair exit into a
catalog-owned Map 20 runtime. The explicit private base view projects both destinations with their
current selection, layout, blocks and area using the accepted shared castle atlas. Map 20 arrival remains explicitly pre-init, with its controlled first-visit
result unselected. F at the palace entrance explicitly applies that result while skipping the scene;
the once-only typed receipt survives further Map 20 traversal, the exact two-input royal return and
subsequent Map 19 movement, and resets in a fresh session. Return facing uses raw warp byte 2/LEFT;
the accepted research graph now derives its matching LEFT annotation from the same warp record.
After the controlled return, the bounded Astral interaction now turns a visible occupied tile into
an open passage: the player faces the actor with a blocked north attempt, then explicitly accepts via F
with the scene skipped. The accepted H2 contract supplies the compiled result; Content binds only the
actor/event fields actually present in the unchanged input. The diagnostic marker and current action
project Application state. The [Astral acceptance boundary](./map03-playability-plan.md#controlled-astral-acceptance-and-passage)
owns its exact input sequence, contract/input distinction, persistence, and private check.
The [west-tower arrival boundary](./map03-playability-plan.md#controlled-west-tower-arrival) continues
the released passage through the exact first Map 19 warp to Map 20 `(6,37)`, preserving controlled
completion and route state. Its required private route check actually executes the existing import;
the public suite's explicit private SKIP does not establish that result.
The [Map 21 arrival boundary](./map03-playability-plan.md#controlled-middle-tower-map-21-arrival)
continues three Left inputs into its exact runtime and separately accepted base-atlas view. It retains the prior
controlled completion and bridge state. The [guard boundary](./map03-playability-plan.md#controlled-map-21-middle-tower-guard)
adds an explicit F preset, the guard's live occupancy shift and a bounded completion receipt for
handler256 and StoryFlag1/F401. Its natural lifecycle and entity135 facing effect remain Unknown.
The [Map 40 arrival boundary](./map03-playability-plan.md#controlled-north-exit-to-map-40)
extends that completed guard state by 18 ordinary inputs into the exact fifth runtime, preserving the
controlled results. Its current display is a traversal diagnostic with the retained base hidden;
the next presentation slice is an independent Map 40 atlas family.
Map 19/20/21/40 general init and event/entity execution remain **Unsupported**; natural caller state,
original rendering, natural facing, and continuity to Battle 01
remain **Unknown / NOT READY**. The known static init shape and bounded route checks are recorded in
the [playability plan](./map03-playability-plan.md#royal-route-acceptance-boundary) and its
[controlled first-visit boundary](./map03-playability-plan.md#controlled-palace-first-visit-result).
The [royal return boundary](./map03-playability-plan.md#royal-return-acceptance-boundary) records the
exact source fields, matching research annotation, rejection rules and controlled private reproduction.
See [Architecture](./architecture.md).
